# API Contracts

This feature is frontend-only. No API contracts required.

All interactions are handled through:
- Gradio component events (Python callbacks)
- localStorage (browser-side preferences)
- CSS variables (theme switching)
